# Board

## Overview

**Placeholder spec — not yet designed.**

The Board page will provide a Kanban-style view of work items organized by state column (e.g. Backlog / Ready / In Progress / Done). Work items displayed here are the same items managed in the Backlog (see `spec.backlog.md`).

This spec must be written and the page built before the Board tab is added to the main tab bar (see `spec.main_tabbar.md`).

---

## Out of Scope for MVP

Everything — this page does not exist in the MVP. The tab is not shown in the tab bar until this spec is complete and the page is implemented.
