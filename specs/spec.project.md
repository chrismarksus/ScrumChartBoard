# Project

## Overview

**Placeholder spec — not yet designed.**

The Project page will provide a Timeline / Gantt view of themes or epics across intervals, mapping to the existing Timelines chart data. It is distinct from the Dashboard's chart view in that it supports direct editing.

This spec must be written and the page built before the Project tab is added to the main tab bar (see `spec.main_tabbar.md`).

---

## Out of Scope for MVP

Everything — this page does not exist in the MVP. The tab is not shown in the tab bar until this spec is complete and the page is implemented.
