# Teams

## Overview

**Placeholder spec — not yet designed.**

The Teams page will manage team member records. The member list it produces will feed the "Assigned To" dropdown on work items (see `spec.work_item.md`), replacing the free-text field used in the MVP.

This spec must be written and the page built before the Teams tab is added to the main tab bar (see `spec.main_tabbar.md`).

---

## Out of Scope for MVP

Everything — this page does not exist in the MVP. The tab is not shown in the tab bar until this spec is complete and the page is implemented. "Assigned To" remains a free-text field until then.
